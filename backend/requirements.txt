# --- runtime -----------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
# Hardened XML parsing for the PubMed (MEDLINE XML) and arXiv (Atom) feeds.
defusedxml==0.7.1

# --- ranking (Stage 2) -------------------------------------------------
# Bi-encoder embeddings for semantic relevance. Pulls in torch, which is
# by far the heaviest dependency here; the app degrades to a hashing
# fallback if the model cannot be loaded.
sentence-transformers==3.3.1
# Okapi BM25 for the lexical half of the hybrid.
rank-bm25==0.2.2
numpy>=1.26,<3

# --- enrichment: NER and clustering (Stage 3) --------------------------
# NER. A SciSpacy biomedical model is preferred when installed; the
# general model is the fallback and its labels are trusted selectively.
# The model itself is a separate download:
#   python -m spacy download en_core_web_sm
spacy>=3.8,<3.9
# Ward agglomerative clustering and the silhouette score.
scikit-learn==1.6.0
